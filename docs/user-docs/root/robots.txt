# robots.txt — docs.theygent.ai
#
# Policy: every crawler — classic search engines and AI/answer engines alike —
# is welcome everywhere on this site. The named groups below make that welcome
# explicit for AI crawlers. Per RFC 9309 a named group REPLACES the * group for
# that bot, so every group here must stay "Allow: /" — never add a Disallow to
# one group without mirroring the decision in every group.
#
# This file lives in docs/user-docs/root/ on the main branch and is copied to the
# deploy tree root by .github/workflows/docs.yml (mike owns the gh-pages root).
# Curated machine-readable summary: https://docs.theygent.ai/llms.txt

User-agent: *
Allow: /

# --- OpenAI (training · ChatGPT search index · user-triggered fetches) ---
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
Allow: /

# --- Anthropic (training · Claude search index · user-triggered fetches) ---
User-agent: ClaudeBot
User-agent: Claude-SearchBot
User-agent: Claude-User
Allow: /

# Legacy Anthropic identifiers still seen in the wild
User-agent: anthropic-ai
User-agent: Claude-Web
Allow: /

# --- Perplexity (index · user-triggered fetches) ---
User-agent: PerplexityBot
User-agent: Perplexity-User
Allow: /

# --- Google AI surfaces (classic Googlebot is covered by * above;
#     Google-Extended is the Gemini training/grounding control token) ---
User-agent: Google-Extended
User-agent: GoogleOther
User-agent: Google-CloudVertexBot
User-agent: Google-NotebookLM
Allow: /

# --- Microsoft Bing (grounds Copilot answers) ---
User-agent: bingbot
Allow: /

# --- Apple (Applebot = Siri/Spotlight; -Extended = Apple Intelligence training) ---
User-agent: Applebot
User-agent: Applebot-Extended
Allow: /

# --- Meta ---
User-agent: meta-externalagent
User-agent: meta-externalfetcher
User-agent: FacebookBot
Allow: /

# --- Amazon (Alexa / Nova) ---
User-agent: Amazonbot
Allow: /

# --- Other AI crawlers and assistants ---
User-agent: CCBot
User-agent: Bytespider
User-agent: cohere-ai
User-agent: cohere-training-data-crawler
User-agent: MistralAI-User
User-agent: DuckAssistBot
User-agent: YouBot
User-agent: AI2Bot
User-agent: PetalBot
User-agent: Diffbot
User-agent: Timpibot
Allow: /

# The stable alias's sitemap — every version's pages canonicalize into these URLs.
Sitemap: https://docs.theygent.ai/latest/sitemap.xml
